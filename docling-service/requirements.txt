# Web framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Task queue
celery[redis]>=5.3.0
redis>=5.0.0

# Docling (document parsing)
docling>=2.0.0

# Object storage (file transfer between API and Worker)
minio>=7.2.0

# PyTorch CPU-only is installed separately in Dockerfile
